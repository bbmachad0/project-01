# ─── Foundation - IAM: Table Optimizer ───────────────────────────
# Per-project Table Optimizer roles are now created inside each
# project stack (see projects/<name>/iam.tf).
#
# This file is intentionally empty.  It is kept as a placeholder
# so the directory structure remains self-documenting.
